# SEO · GEO · Meta Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 퍼블릭 사이트 전체에 일관된 SEO 메타·OG, JSON-LD, `llms.txt`/`llms-full.txt`, AI 친화 robots를 넣고 Site URL을 `NEXT_PUBLIC_SITE_URL`(예정 `https://aic.kr`)로 분리한다.

**Architecture:** `src/lib/seo/`에 `getSiteUrl` · `pageMetadata` · JSON-LD 빌더를 두고 페이지는 얇게 호출한다. GEO는 App Router 텍스트 라우트, Admin은 noindex. Auth용 `AUTH_URL`과 SEO base를 분리한다.

**Tech Stack:** Next.js 16 Metadata API · MetadataRoute · App Router route handlers · Prisma(Insights/settings) · pnpm

## Global Constraints

- Spec: `docs/superpowers/specs/2026-07-29-seo-geo-design.md` (**approved**)
- 접근안 **1**: 중앙 SEO 모듈 + 페이지 헬퍼 (Admin SEO CMS · hreflang · Event/FAQ 스키마 금지)
- Site URL 우선순위: `NEXT_PUBLIC_SITE_URL` → `AUTH_URL` → `http://localhost:3000`
- 예정 도메인: `https://aic.kr` (문서·env 주석만; DNS는 P5)
- 시크릿·`.env` 실값 커밋 금지
- 출하: feature 브랜치 + PR (`finishing-a-development-branch`)
- 브랜치: `feat/seo-geo-meta` · base: `main`

---

## File Structure

| Path | Responsibility |
|---|---|
| `src/lib/seo/site.ts` | `getSiteUrl()`, 브랜드 상수 |
| `src/lib/seo/metadata.ts` | `pageMetadata(...)` → `Metadata` |
| `src/lib/seo/json-ld.ts` | Organization / WebSite / BreadcrumbList / Article |
| `src/lib/seo/llms.ts` | llms.txt / llms-full.txt 본문 생성 |
| `src/components/seo/JsonLd.tsx` | JSON-LD script 태그 |
| `src/app/layout.tsx` | 루트 metadata + Organization |
| `src/app/(public)/page.tsx` 등 | 페이지별 `pageMetadata` + Breadcrumb/WebSite |
| `src/app/(public)/insights/[id]/page.tsx` | Article 메타 + JSON-LD |
| `src/app/admin/layout.tsx` | Admin `robots: noindex` (신규 layout) |
| `src/app/sitemap.ts` / `robots.ts` | `getSiteUrl` + AI allow |
| `src/app/llms.txt/route.ts` | GEO 요약 |
| `src/app/llms-full.txt/route.ts` | GEO 확장 + Insights |
| `public/og-default.png` | 1200×630 OG (SVG 대체 기본) |
| `.env.example` / `.env.vercel.example` / `README.md` | `NEXT_PUBLIC_SITE_URL` 문서화 |

---

### Task 1: Feature 브랜치 + 스펙 커밋

**Files:**
- Create(already untracked): `docs/superpowers/specs/2026-07-29-seo-geo-design.md`
- Create: `docs/superpowers/plans/2026-07-29-seo-geo.md` (이 플랜)

**Interfaces:**
- Consumes: approved spec
- Produces: 브랜치 `feat/seo-geo-meta` + 스펙/플랜 커밋

- [ ] **Step 1: 브랜치 생성**

```bash
cd /Users/chunghyo/aic_website
git checkout main
git pull --ff-only
git checkout -b feat/seo-geo-meta
```

Expected: `Switched to a new branch 'feat/seo-geo-meta'`

- [ ] **Step 2: 스펙·플랜 스테이징 후 커밋**

```bash
git add docs/superpowers/specs/2026-07-29-seo-geo-design.md \
  docs/superpowers/plans/2026-07-29-seo-geo.md
git commit -m "$(cat <<'EOF'
docs: SEO·GEO·Meta 스펙 및 구현 플랜

EOF
)"
```

Expected: 커밋 성공

---

### Task 2: `src/lib/seo/site.ts` + 단위 검증

**Files:**
- Create: `src/lib/seo/site.ts`
- Create: `src/lib/seo/site.test.ts` (Node built-in test)

**Interfaces:**
- Consumes: `process.env.NEXT_PUBLIC_SITE_URL`, `process.env.AUTH_URL`
- Produces:
  - `getSiteUrl(): string` — trailing slash 제거한 absolute origin
  - `SITE_NAME = "AI Collective Seoul"`
  - `SITE_NAME_SHORT = "AIC Seoul"`
  - `DEFAULT_DESCRIPTION = "AI 시대를 혼자 따라가지 않아도 되는 커뮤니티 — AIC 서울 챕터"`
  - `DEFAULT_OG_IMAGE = "/og-default.png"`
  - `THEME_COLOR = "#0F0C0A"`

- [ ] **Step 1: 테스트 작성**

```ts
// src/lib/seo/site.test.ts
import { describe, it } from "node:test";
import assert from "node:assert/strict";
import { getSiteUrl } from "./site";

describe("getSiteUrl", () => {
  it("prefers NEXT_PUBLIC_SITE_URL", () => {
    process.env.NEXT_PUBLIC_SITE_URL = "https://aic.kr/";
    delete process.env.AUTH_URL;
    assert.equal(getSiteUrl(), "https://aic.kr");
  });

  it("falls back to AUTH_URL", () => {
    delete process.env.NEXT_PUBLIC_SITE_URL;
    process.env.AUTH_URL = "https://example.vercel.app";
    assert.equal(getSiteUrl(), "https://example.vercel.app");
  });

  it("defaults to localhost", () => {
    delete process.env.NEXT_PUBLIC_SITE_URL;
    delete process.env.AUTH_URL;
    assert.equal(getSiteUrl(), "http://localhost:3000");
  });
});
```

- [ ] **Step 2: 실패 확인**

```bash
node --import tsx --test src/lib/seo/site.test.ts
```

Expected: FAIL (module not found) — `tsx`가 없으면 `pnpm exec tsx --test src/lib/seo/site.test.ts` 사용 (`tsx`는 이미 devDependency)

- [ ] **Step 3: 구현**

```ts
// src/lib/seo/site.ts
export const SITE_NAME = "AI Collective Seoul";
export const SITE_NAME_SHORT = "AIC Seoul";
export const DEFAULT_DESCRIPTION =
  "AI 시대를 혼자 따라가지 않아도 되는 커뮤니티 — AIC 서울 챕터";
export const DEFAULT_OG_IMAGE = "/og-default.png";
export const THEME_COLOR = "#0F0C0A";

export function getSiteUrl(): string {
  const raw =
    process.env.NEXT_PUBLIC_SITE_URL?.trim() ||
    process.env.AUTH_URL?.trim() ||
    "http://localhost:3000";
  return raw.replace(/\/$/, "");
}
```

- [ ] **Step 4: 테스트 통과**

```bash
pnpm exec tsx --test src/lib/seo/site.test.ts
```

Expected: 3 pass

- [ ] **Step 5: Commit**

```bash
git add src/lib/seo/site.ts src/lib/seo/site.test.ts
git commit -m "$(cat <<'EOF'
feat(seo): add getSiteUrl and brand constants

EOF
)"
```

---

### Task 3: `pageMetadata` 헬퍼

**Files:**
- Create: `src/lib/seo/metadata.ts`
- Create: `src/lib/seo/metadata.test.ts`

**Interfaces:**
- Consumes: `getSiteUrl`, `DEFAULT_*` from `site.ts`
- Produces:
```ts
export type PageMetadataInput = {
  title: string;
  description: string;
  path: string; // e.g. "/" | "/meetups" | "/insights/abc"
  image?: string;
  type?: "website" | "article";
  absoluteTitle?: boolean;
  publishedTime?: string; // ISO, article only
  robots?: Metadata["robots"];
};

export function pageMetadata(input: PageMetadataInput): Metadata;
```

- [ ] **Step 1: 테스트**

```ts
// src/lib/seo/metadata.test.ts
import { describe, it } from "node:test";
import assert from "node:assert/strict";
import { pageMetadata } from "./metadata";

describe("pageMetadata", () => {
  it("sets canonical and OG url from path", () => {
    process.env.NEXT_PUBLIC_SITE_URL = "https://aic.kr";
    const m = pageMetadata({
      title: "Meetups",
      description: "모임 설명",
      path: "/meetups",
    });
    assert.equal(m.alternates?.canonical, "https://aic.kr/meetups");
    assert.equal(m.openGraph?.url, "https://aic.kr/meetups");
    assert.equal(m.description, "모임 설명");
  });

  it("uses absolute title when requested", () => {
    const m = pageMetadata({
      title: "AI Collective Seoul",
      description: "x",
      path: "/",
      absoluteTitle: true,
    });
    assert.deepEqual(m.title, { absolute: "AI Collective Seoul" });
  });
});
```

- [ ] **Step 2: 실패 확인** → `pnpm exec tsx --test src/lib/seo/metadata.test.ts`

- [ ] **Step 3: 구현**

```ts
// src/lib/seo/metadata.ts
import type { Metadata } from "next";
import {
  DEFAULT_DESCRIPTION,
  DEFAULT_OG_IMAGE,
  SITE_NAME,
  getSiteUrl,
} from "./site";

export type PageMetadataInput = {
  title: string;
  description: string;
  path: string;
  image?: string;
  type?: "website" | "article";
  absoluteTitle?: boolean;
  publishedTime?: string;
  robots?: Metadata["robots"];
};

function absolutePath(path: string): string {
  const base = getSiteUrl();
  if (path === "/" || path === "") return `${base}/`;
  return `${base}${path.startsWith("/") ? path : `/${path}`}`;
}

export function pageMetadata(input: PageMetadataInput): Metadata {
  const url = absolutePath(input.path);
  const image = input.image ?? DEFAULT_OG_IMAGE;
  const type = input.type ?? "website";
  const title = input.absoluteTitle
    ? { absolute: input.title }
    : input.title;

  return {
    title,
    description: input.description || DEFAULT_DESCRIPTION,
    alternates: { canonical: url },
    robots: input.robots,
    openGraph: {
      type,
      locale: "ko_KR",
      siteName: SITE_NAME,
      title: input.title,
      description: input.description,
      url,
      images: [{ url: image, width: 1200, height: 630 }],
      ...(type === "article" && input.publishedTime
        ? { publishedTime: input.publishedTime }
        : {}),
    },
    twitter: {
      card: "summary_large_image",
      title: input.title,
      description: input.description,
      images: [image],
    },
  };
}
```

- [ ] **Step 4: 테스트 통과**

```bash
pnpm exec tsx --test src/lib/seo/metadata.test.ts
```

- [ ] **Step 5: Commit**

```bash
git add src/lib/seo/metadata.ts src/lib/seo/metadata.test.ts
git commit -m "$(cat <<'EOF'
feat(seo): add pageMetadata helper for canonical and OG

EOF
)"
```

---

### Task 4: JSON-LD 빌더 + `JsonLd` 컴포넌트

**Files:**
- Create: `src/lib/seo/json-ld.ts`
- Create: `src/components/seo/JsonLd.tsx`

**Interfaces:**
- Consumes: `getSiteUrl`, `SITE_NAME`, `DEFAULT_DESCRIPTION`, `DEFAULT_OG_IMAGE`
- Produces:
```ts
export function organizationJsonLd(opts?: { sameAs?: string[] }): Record<string, unknown>;
export function websiteJsonLd(): Record<string, unknown>;
export function breadcrumbJsonLd(items: { name: string; path: string }[]): Record<string, unknown>;
export function articleJsonLd(input: {
  title: string;
  description: string;
  path: string;
  image?: string | null;
  datePublished?: Date | string | null;
  dateModified?: Date | string | null;
}): Record<string, unknown>;
```

- [ ] **Step 1: `json-ld.ts` 구현**

```ts
// src/lib/seo/json-ld.ts
import {
  DEFAULT_DESCRIPTION,
  DEFAULT_OG_IMAGE,
  SITE_NAME,
  getSiteUrl,
} from "./site";

function abs(path: string): string {
  const base = getSiteUrl();
  if (path === "/" || path === "") return `${base}/`;
  return `${base}${path.startsWith("/") ? path : `/${path}`}`;
}

export function organizationJsonLd(opts?: { sameAs?: string[] }) {
  const sameAs = (opts?.sameAs ?? []).filter(Boolean);
  return {
    "@context": "https://schema.org",
    "@type": "Organization",
    name: SITE_NAME,
    url: getSiteUrl(),
    description: DEFAULT_DESCRIPTION,
    logo: abs(DEFAULT_OG_IMAGE),
    ...(sameAs.length ? { sameAs } : {}),
  };
}

export function websiteJsonLd() {
  return {
    "@context": "https://schema.org",
    "@type": "WebSite",
    name: SITE_NAME,
    url: getSiteUrl(),
    inLanguage: "ko",
    publisher: { "@type": "Organization", name: SITE_NAME, url: getSiteUrl() },
  };
}

export function breadcrumbJsonLd(items: { name: string; path: string }[]) {
  return {
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    itemListElement: items.map((item, i) => ({
      "@type": "ListItem",
      position: i + 1,
      name: item.name,
      item: abs(item.path),
    })),
  };
}

export function articleJsonLd(input: {
  title: string;
  description: string;
  path: string;
  image?: string | null;
  datePublished?: Date | string | null;
  dateModified?: Date | string | null;
}) {
  const toIso = (v?: Date | string | null) =>
    v ? (v instanceof Date ? v.toISOString() : v) : undefined;
  return {
    "@context": "https://schema.org",
    "@type": "Article",
    headline: input.title,
    description: input.description,
    image: abs(input.image || DEFAULT_OG_IMAGE),
    mainEntityOfPage: abs(input.path),
    datePublished: toIso(input.datePublished),
    dateModified: toIso(input.dateModified) ?? toIso(input.datePublished),
    author: { "@type": "Organization", name: SITE_NAME, url: getSiteUrl() },
    publisher: { "@type": "Organization", name: SITE_NAME, url: getSiteUrl() },
  };
}
```

- [ ] **Step 2: `JsonLd` 컴포넌트**

```tsx
// src/components/seo/JsonLd.tsx
type Props = { data: Record<string, unknown> | Record<string, unknown>[] };

export function JsonLd({ data }: Props) {
  return (
    <script
      type="application/ld+json"
      dangerouslySetInnerHTML={{ __html: JSON.stringify(data) }}
    />
  );
}
```

- [ ] **Step 3: Commit**

```bash
git add src/lib/seo/json-ld.ts src/components/seo/JsonLd.tsx
git commit -m "$(cat <<'EOF'
feat(seo): add JSON-LD builders and JsonLd component

EOF
)"
```

---

### Task 5: OG PNG + env 문서

**Files:**
- Create: `public/og-default.png` (1200×630)
- Modify: `.env.example`, `.env.vercel.example`, `README.md` (env 표)

**Interfaces:**
- Consumes: 기존 `public/og-default.svg` 디자인(#0F0C0A / #C7A77E / #EEE9DF)
- Produces: PNG 기본 OG; env 키 `NEXT_PUBLIC_SITE_URL`

- [ ] **Step 1: PNG 생성**

`sharp`가 있으면:

```bash
pnpm exec tsx -e "
const sharp = require('sharp');
const svg = \`<svg xmlns='http://www.w3.org/2000/svg' width='1200' height='630'>
  <rect width='1200' height='630' fill='#0F0C0A'/>
  <rect width='1200' height='8' fill='#C7A77E'/>
  <text x='80' y='280' fill='#EEE9DF' font-family='Georgia, serif' font-size='64'>AI Collective Seoul</text>
  <text x='80' y='360' fill='#C7A77E' font-family='system-ui, sans-serif' font-size='28'>Better questions come from conversation.</text>
</svg>\`;
sharp(Buffer.from(svg)).png().toFile('public/og-default.png').then(() => console.log('ok'));
"
```

sharp 없으면: SVG를 브라우저/디자인 툴로 PNG export해 `public/og-default.png`에 저장 (1200×630). SVG 파일은 유지.

- [ ] **Step 2: `.env.example`에 추가** (`AUTH_URL` 아래)

```
# Public site origin for SEO/canonical/OG (planned prod: https://aic.kr)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
```

`.env.vercel.example`에도 동일 키 + 주석 `# Production: https://aic.kr (until DNS: https://YOUR-PROJECT.vercel.app)`

- [ ] **Step 3: README env 표에 `NEXT_PUBLIC_SITE_URL` 행 추가**

- [ ] **Step 4: Commit**

```bash
git add public/og-default.png .env.example .env.vercel.example README.md
git commit -m "$(cat <<'EOF'
feat(seo): add OG PNG and NEXT_PUBLIC_SITE_URL docs

EOF
)"
```

---

### Task 6: 루트 layout · sitemap · robots

**Files:**
- Modify: `src/app/layout.tsx`
- Modify: `src/app/sitemap.ts`
- Modify: `src/app/robots.ts`

**Interfaces:**
- Consumes: `getSiteUrl`, 브랜드 상수, `organizationJsonLd`, `JsonLd`, `DEFAULT_OG_IMAGE`, `THEME_COLOR`
- Produces: 루트 metadata(키워드·robots·themeColor·verification hook) · sitemap/robots가 `getSiteUrl` 사용 · AI bots allow

- [ ] **Step 1: `layout.tsx` metadata를 헬퍼 기반으로 교체**

핵심 필드:

```ts
import type { Metadata, Viewport } from "next";
import { JsonLd } from "@/components/seo/JsonLd";
import { organizationJsonLd } from "@/lib/seo/json-ld";
import {
  DEFAULT_DESCRIPTION,
  DEFAULT_OG_IMAGE,
  SITE_NAME,
  THEME_COLOR,
  getSiteUrl,
} from "@/lib/seo/site";

const siteUrl = getSiteUrl();

export const metadata: Metadata = {
  metadataBase: new URL(siteUrl),
  title: { default: SITE_NAME, template: `%s · ${SITE_NAME}` },
  description: DEFAULT_DESCRIPTION,
  keywords: [
    "AI Collective",
    "AIC Seoul",
    "AI 커뮤니티",
    "서울 AI",
    "The AI Collective",
  ],
  authors: [{ name: SITE_NAME }],
  creator: SITE_NAME,
  publisher: SITE_NAME,
  robots: {
    index: true,
    follow: true,
    googleBot: { index: true, follow: true, "max-image-preview": "large" },
  },
  openGraph: {
    type: "website",
    locale: "ko_KR",
    siteName: SITE_NAME,
    title: SITE_NAME,
    description: DEFAULT_DESCRIPTION,
    images: [{ url: DEFAULT_OG_IMAGE, width: 1200, height: 630 }],
  },
  twitter: {
    card: "summary_large_image",
    title: SITE_NAME,
    description: DEFAULT_DESCRIPTION,
    images: [DEFAULT_OG_IMAGE],
  },
  ...(process.env.GOOGLE_SITE_VERIFICATION
    ? { verification: { google: process.env.GOOGLE_SITE_VERIFICATION } }
    : {}),
};

export const viewport: Viewport = {
  themeColor: THEME_COLOR,
};
```

본문: 인라인 JSON-LD 제거 → `<JsonLd data={organizationJsonLd()} />` (sameAs는 settings 비동기라 루트에서는 비움 또는 정적 LinkedIn 생략 — Home에서 sameAs 보강 가능. **루트는 sameAs 없이 Organization만**).

- [ ] **Step 2: `sitemap.ts`의 `baseUrl()`을 `getSiteUrl`로 교체**

```ts
import { getSiteUrl } from "@/lib/seo/site";
// ...
const base = getSiteUrl();
```

- [ ] **Step 3: `robots.ts` 교체**

```ts
import type { MetadataRoute } from "next";
import { getSiteUrl } from "@/lib/seo/site";

const AI_BOTS = [
  "GPTBot",
  "ChatGPT-User",
  "ClaudeBot",
  "PerplexityBot",
  "Google-Extended",
  "Applebot-Extended",
] as const;

export default function robots(): MetadataRoute.Robots {
  const base = getSiteUrl();
  return {
    rules: [
      {
        userAgent: "*",
        allow: "/",
        disallow: ["/admin", "/api"],
      },
      ...AI_BOTS.map((userAgent) => ({
        userAgent,
        allow: "/",
        disallow: ["/admin", "/api"],
      })),
    ],
    sitemap: `${base}/sitemap.xml`,
  };
}
```

- [ ] **Step 4: Commit**

```bash
git add src/app/layout.tsx src/app/sitemap.ts src/app/robots.ts
git commit -m "$(cat <<'EOF'
feat(seo): wire root metadata, sitemap, and AI-friendly robots

EOF
)"
```

---

### Task 7: 퍼블릭 페이지 metadata + Breadcrumb JSON-LD

**Files:**
- Modify: `src/app/(public)/page.tsx`
- Modify: `src/app/(public)/meetups/page.tsx`
- Modify: `src/app/(public)/people/page.tsx`
- Modify: `src/app/(public)/insights/page.tsx`
- Modify: `src/app/(public)/contact/page.tsx`

**Interfaces:**
- Consumes: `pageMetadata`, `JsonLd`, `websiteJsonLd`, `breadcrumbJsonLd`, `organizationJsonLd`, `SITE_NAME`, `getSiteSettingsMap` (Home sameAs)
- Produces: 각 페이지 canonical·OG·breadcrumb; Home은 absolute title + WebSite(+Organization sameAs)

- [ ] **Step 1: Home**

```ts
export const metadata = pageMetadata({
  title: SITE_NAME,
  description:
    "AI 시대, 좋은 질문은 대화에서 나옵니다. AIC 서울 챕터 공식 사이트.",
  path: "/",
  absoluteTitle: true,
});
```

페이지 JSX 상단:

```tsx
const linkedin = settings["social.linkedin"];
<JsonLd
  data={[
    websiteJsonLd(),
    organizationJsonLd({
      sameAs: linkedin ? [linkedin] : [],
    }),
  ]}
/>
```

- [ ] **Step 2: Meetups / People / Insights / Contact**

각기:

| path | title | description |
|---|---|---|
| `/meetups` | Meetups | `AIC 서울 챕터의 정기 모임과 원데이 클래스, 지난 순간의 기록.` |
| `/people` | People | `AIC 서울 챕터를 함께 만드는 사람들.` |
| `/insights` | Insights | `모임 기록, 클래스 노트, 커뮤니티 이야기를 남깁니다.` |
| `/contact` | Contact | `협업·후원, 교육, 커뮤니티 참여 문의.` |

각 페이지에:

```tsx
<JsonLd
  data={breadcrumbJsonLd([
    { name: "Home", path: "/" },
    { name: "Meetups", path: "/meetups" }, // 페이지에 맞게
  ])}
/>
```

- [ ] **Step 3: Commit**

```bash
git add src/app/\(public\)/page.tsx \
  src/app/\(public\)/meetups/page.tsx \
  src/app/\(public\)/people/page.tsx \
  src/app/\(public\)/insights/page.tsx \
  src/app/\(public\)/contact/page.tsx
git commit -m "$(cat <<'EOF'
feat(seo): apply pageMetadata and breadcrumbs on public pages

EOF
)"
```

---

### Task 8: Insights 상세 Article 메타 + JSON-LD

**Files:**
- Modify: `src/app/(public)/insights/[id]/page.tsx`

**Interfaces:**
- Consumes: `pageMetadata`, `articleJsonLd`, `breadcrumbJsonLd`, `JsonLd`, `getInsightById`
- Produces: article OG · publishedTime · Article + Breadcrumb JSON-LD

- [ ] **Step 1: `generateMetadata`를 `pageMetadata`로 교체**

```ts
export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const { id } = await params;
  const post = await getInsightById(id);
  if (!post) return pageMetadata({ title: "Insights", description: "", path: "/insights" });
  return pageMetadata({
    title: post.title,
    description: post.summary,
    path: `/insights/${post.id}`,
    image: post.thumbnailUrl || undefined,
    type: "article",
    publishedTime: post.publishedAt?.toISOString(),
  });
}
```

- [ ] **Step 2: 페이지에 JSON-LD 삽입**

```tsx
<JsonLd
  data={[
    breadcrumbJsonLd([
      { name: "Home", path: "/" },
      { name: "Insights", path: "/insights" },
      { name: post.title, path: `/insights/${post.id}` },
    ]),
    articleJsonLd({
      title: post.title,
      description: post.summary,
      path: `/insights/${post.id}`,
      image: post.thumbnailUrl,
      datePublished: post.publishedAt,
      dateModified: post.updatedAt,
    }),
  ]}
/>
```

- [ ] **Step 3: Commit**

```bash
git add src/app/\(public\)/insights/\[id\]/page.tsx
git commit -m "$(cat <<'EOF'
feat(seo): article metadata and JSON-LD for insight posts

EOF
)"
```

---

### Task 9: Admin noindex layout

**Files:**
- Create: `src/app/admin/layout.tsx`

**Interfaces:**
- Consumes: Next `Metadata`
- Produces: `/admin/**` 전체에 `robots: { index: false, follow: false }`

- [ ] **Step 1: 생성**

```tsx
// src/app/admin/layout.tsx
import type { Metadata } from "next";

export const metadata: Metadata = {
  robots: { index: false, follow: false },
};

export default function AdminRootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
```

기존 `(console)/layout.tsx` · auth 페이지는 그대로 children 통과.

- [ ] **Step 2: Commit**

```bash
git add src/app/admin/layout.tsx
git commit -m "$(cat <<'EOF'
feat(seo): noindex all admin routes

EOF
)"
```

---

### Task 10: `llms.txt` / `llms-full.txt`

**Files:**
- Create: `src/lib/seo/llms.ts`
- Create: `src/app/llms.txt/route.ts`
- Create: `src/app/llms-full.txt/route.ts`

**Interfaces:**
- Consumes: `getSiteUrl`, `SITE_NAME`, `DEFAULT_DESCRIPTION`, Prisma insights(optional)
- Produces: `text/plain; charset=utf-8` 응답

- [ ] **Step 1: `llms.ts` 본문 빌더**

```ts
// src/lib/seo/llms.ts
import { DEFAULT_DESCRIPTION, SITE_NAME, getSiteUrl } from "./site";

export function buildLlmsTxt(): string {
  const base = getSiteUrl();
  return `# ${SITE_NAME}

> ${DEFAULT_DESCRIPTION}

The AI Collective Seoul Chapter (AIC Seoul) is the Seoul chapter of The AI Collective.
Prefer citing this site as "${SITE_NAME}" or "AIC Seoul".

## Pages

- [Home](${base}/): Chapter overview and why we gather
- [Meetups](${base}/meetups): Monthly meetups and one-day classes
- [People](${base}/people): Organizing members
- [Insights](${base}/insights): Meetup recaps and community notes
- [Contact](${base}/contact): Partnership, education, and community inquiries

## Optional

- [Full text](${base}/llms-full.txt): Longer page summaries and published insights
`;
}

export function buildLlmsFullTxt(posts: { id: string; title: string; summary: string }[]): string {
  const base = getSiteUrl();
  const staticPart = `${buildLlmsTxt()}

## Page summaries

- Home: Offline AI community in Seoul — conversation over solo chasing of AI news.
- Meetups: Monthly offline meetup format plus one-day classes and photo archive.
- People: Visible chapter organizers.
- Insights: Published recaps and notes.
- Contact: Collaboration, sponsorship, education, community join.

## Published insights
`;
  if (!posts.length) {
    return `${staticPart}\n(No published insights yet.)\n`;
  }
  const list = posts
    .map((p) => `- [${p.title}](${base}/insights/${p.id}): ${p.summary}`)
    .join("\n");
  return `${staticPart}\n${list}\n`;
}
```

- [ ] **Step 2: 라우트**

```ts
// src/app/llms.txt/route.ts
import { buildLlmsTxt } from "@/lib/seo/llms";

export const dynamic = "force-dynamic";

export function GET() {
  return new Response(buildLlmsTxt(), {
    headers: {
      "Content-Type": "text/plain; charset=utf-8",
      "Cache-Control": "public, max-age=3600",
    },
  });
}
```

```ts
// src/app/llms-full.txt/route.ts
import { prisma } from "@/lib/db";
import { buildLlmsFullTxt } from "@/lib/seo/llms";

export const dynamic = "force-dynamic";

export async function GET() {
  let posts: { id: string; title: string; summary: string }[] = [];
  if (process.env.DATABASE_URL) {
    try {
      posts = await prisma.insightPost.findMany({
        where: { status: "published" },
        select: { id: true, title: true, summary: true },
        orderBy: { publishedAt: "desc" },
        take: 50,
      });
    } catch {
      posts = [];
    }
  }
  return new Response(buildLlmsFullTxt(posts), {
    headers: {
      "Content-Type": "text/plain; charset=utf-8",
      "Cache-Control": "public, max-age=3600",
    },
  });
}
```

- [ ] **Step 3: Commit**

```bash
git add src/lib/seo/llms.ts src/app/llms.txt/route.ts src/app/llms-full.txt/route.ts
git commit -m "$(cat <<'EOF'
feat(geo): add llms.txt and llms-full.txt routes

EOF
)"
```

---

### Task 11: 빌드 · 수동 검수 · PR

**Files:**
- None (verification + finishing)

**Interfaces:**
- Consumes: 위 전 Task 산출물
- Produces: 통과한 빌드 + PR URL

- [ ] **Step 1: 단위 테스트 + lint + build**

```bash
pnpm exec tsx --test src/lib/seo/*.test.ts
pnpm lint
pnpm build
```

Expected: tests pass, lint clean, build success

- [ ] **Step 2: dev 서버에서 확인** (`pnpm dev`)

| URL | 기대 |
|---|---|
| `/` view-source | `og:image` → `/og-default.png`, absolute title, WebSite JSON-LD |
| `/meetups` | canonical + BreadcrumbList |
| `/insights/[id]` | Article OG + Article JSON-LD |
| `/sitemap.xml` | 정적 경로 (+ insights if DB) |
| `/robots.txt` | GPTBot allow, Disallow `/admin` |
| `/llms.txt` | text/plain, `aic.kr` 또는 localhost 링크 |
| `/llms-full.txt` | 200 |
| `/admin/login` | `noindex` |

- [ ] **Step 3: finishing — Push + PR**

`finishing-a-development-branch` 스킬 따름. PR 본문에 스펙 링크·검수 체크리스트 포함.

```bash
git push -u origin HEAD
gh pr create --title "feat: SEO·GEO·Meta 전면 보강" --body "$(cat <<'EOF'
## Summary
- 중앙 `src/lib/seo` + 페이지별 canonical/OG/Twitter
- JSON-LD (Organization, WebSite, Breadcrumb, Article)
- `llms.txt` / `llms-full.txt` + AI crawler allow in robots
- `NEXT_PUBLIC_SITE_URL` (예정 https://aic.kr)

## Spec
docs/superpowers/specs/2026-07-29-seo-geo-design.md

## Test plan
- [ ] pnpm build
- [ ] /sitemap.xml /robots.txt /llms.txt
- [ ] 공개 페이지 head: canonical + og tags
- [ ] Insights 상세 Article JSON-LD
- [ ] /admin noindex

EOF
)"
```

---

## Spec coverage (self-review)

| Spec 절 | Task |
|---|---|
| §2 URL · env | 2, 5, 6 |
| §3 아키텍처 `src/lib/seo` | 2–4, 10 |
| §4 메타 · OG · PNG · absolute home | 3, 5–8 |
| §5 JSON-LD | 4, 6–8 |
| §6 GEO llms · robots AI | 6, 10 |
| §7 sitemap | 6 |
| Admin noindex | 9 |
| §9 비범위 준수 | 전 Task (CMS/hreflang/Event 없음) |
| 출하 PR | 1, 11 |
